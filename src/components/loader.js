import React from 'react';
import '../styles/loader.css'; // optional CSS for styling

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;